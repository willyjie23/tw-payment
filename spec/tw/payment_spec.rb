# frozen_string_literal: true

RSpec.describe Tw::Payment do
  it "has a version number" do
    expect(Tw::Payment::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
